{
  "name": "shows-crawler-service",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "prisma": {
    "schema": "./prisma/schema.prisma"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node --transpile-only src/index.ts",
    "dev:watch": "nodemon --exec ts-node src/index.ts",
    "server:dev": "ts-node --transpile-only src/api/server.ts",
    "proxy:dev": "ts-node --transpile-only src/crawlers/proxy.ts",
    "migrate": "npx prisma migrate dev init",
    "seed": "ts-node --transpile-only prisma/db/seed.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "axios": "^1.4.0",
    "fastify": "^4.19.2",
    "node-cron": "^3.0.2",
    "puppeteer": "^20.5.0",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.8",
    "nodemon": "^2.0.22",
    "prisma": "^4.16.2",
    "ts-node": "^10.9.1"
  }
}
